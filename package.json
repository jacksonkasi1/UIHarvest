{
  "name": "ui-harvest",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*",
    "tooling/*"
  ],
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "test": "bun test",
    "typecheck": "turbo typecheck",
    "generate:snapshot": "bun run apps/web/scripts/generate-base-snapshot.ts"
  },
  "dependencies": {},
  "devDependencies": {
    "bun-types": "^1.1.0",
    "turbo": "^2.5.8",
    "typescript": "^5.6.0"
  }
}
